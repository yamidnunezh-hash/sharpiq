web: uvicorn sharpiq-engine.api.main:app --host 0.0.0.0 --port $PORT
# worker desactivado — el motor corre via GitHub Actions (motor.yml, 4x/día)
# para reactivar: worker: python sharpiq-engine/motor_cron.py
